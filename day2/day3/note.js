/**

Truthy and falsy are concepts in JavaScript that determine the boolean value of an expression when evaluated in a boolean context (such as in an if statement or as a condition in a loop).

Truthy Values
In JavaScript, a value is considered "truthy" if it coerces to true when evaluated in a boolean context.

Examples of truthy values:

All non-empty strings ("hello", "0", "false", etc.)
All numbers except for 0 and NaN (e.g., 1, -1, 0.5)
All objects (including arrays and functions)
The boolean value true
Any non-null object or array


Falsy Values
Conversely, a value is considered "falsy" if it coerces to false when evaluated in a boolean context.

Examples of falsy values:

The boolean value false
The number 0 (zero)
The empty string ""
NaN (Not-a-Number)
null
undefined 


 */